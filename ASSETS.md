# 素材说明

`docs/assets/background.webp` 和 `cover.webp` 沿用本次对话中用户提供的参考截图，以及前版网页对该截图的裁取结果。截图对应作品的授权范围未经核实。本包未将这些图片标记为原创、开源或可商用素材。

`docs/assets/ambient-demo.mp3` 沿用前版网页内置的合成氛围试听；它不属于通过访客本地文件选择器添加的歌曲。

`docs/assets/favicon.svg` 是本次制作的简单唱片图标。网页不包含额外字体文件。

公开发布前，可将默认照片与试听音频替换为你有权使用的素材。替换图片时保持文件名，或同步修改 `docs/index.html` 中的引用。替换默认音频时，也应修改 `docs/assets/app.js` 的试听曲目名称与说明。

此包不包含浏览器 IndexedDB、本地已上传的个人歌单、账号令牌、密码、环境变量文件或旧版网页备份。
